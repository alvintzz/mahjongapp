// Intentionally empty: each subproject declares its own plugins so that
// `:scoring` (a pure-Kotlin/JVM module) can be built and tested without needing
// the Android Gradle Plugin or network access to Google's Maven repository.
